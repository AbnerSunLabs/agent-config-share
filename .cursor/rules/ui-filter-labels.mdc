---
description: 筛选类别文案放在控件左侧，不塞进下拉内部
alwaysApply: true
---

# 筛选类别文案位置

「宿主」「域」等类别文案：

- 放在对应下拉框**左侧**，与控件水平对齐。
- **禁止**把类别文案收进下拉触发器内部（不要做成 kicker / 前缀芯片去挤占选择值区域）。
- 不要用英文表单那套 `uppercase` + 字距去排中文标签。
