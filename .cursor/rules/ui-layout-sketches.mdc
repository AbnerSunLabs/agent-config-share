---
description: 页面布局与按钮必须同时给出文字和草图
alwaysApply: true
---

# UI 布局必须配草图

讨论或定稿页面布局、按钮、弹窗、筛选条时：

- 除文字说明外，**必须**附 ASCII / 框线图草图（主界面、确认框、空态等关键屏各一张）。
- 禁止只写「顶栏有筛选、卡片上有按钮」而不画结构。
- 用户已拒绝浏览器 mockup 时，用对话里的字符草图即可，不要改用本地网页演示。

```text
# ✅ 同时有结构草图
┌─ 顶栏 [检查差异] [写入同步] ─┐
│  [MCP卡] [Hooks卡] [Skills卡] │
└──────────────────────────────┘

# ❌ 只有文字
# 「顶部是筛选，中间是卡片」
```
