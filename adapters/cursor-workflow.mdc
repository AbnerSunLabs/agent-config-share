---
description: 跨 Agent 个人研发工作流入口
alwaysApply: true
---

# Cursor 工作流入口

本规则只映射 Cursor 能力。核心流程以 `.agent-workflow/WORKFLOW.md` 为唯一事实源。

## 必读顺序

1. `.agent-workflow/WORKFLOW.md`
2. `.agent-workflow/PROJECT.md`
3. 当前任务 `.agent-workflow/tasks/<task-id>.md`
4. 项目声明的架构和测试文档

## 执行规则

- 先探索真实项目，再填写任务的规划与设计。
- 规划设计完成后等待人工批准；未批准时不修改业务源码。
- 批准后自主完成实现、验证、Review、修复、复验和交付。
- 使用项目真实命令生成验证证据。
- Review 必须缺陷优先；无法使用独立 Agent 时按统一 Review 模板执行。
- 默认交付级别 L2；未经 L3 授权禁止 Push 或创建 PR。
- 遇到授权范围变化、高风险操作、外部权限缺失或 3 轮修复失败时进入 blocked。
- 不复制或重写核心流程；需要修改规则时修改 `.agent-workflow/WORKFLOW.md`。

## 降级

- 无 Agent 或自动化能力时串行执行同一流程。
- 无 Git 仓库时 L2 降级为 L1。
- 无法执行检查时记录精确原因和影响，不得伪造通过结果。

